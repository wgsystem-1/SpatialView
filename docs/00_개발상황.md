# SpatialView 개발 상황 보고서

**문서 작성일**: 2026년 01월 07일
**프로젝트 버전**: MVP 1.0  
**개발 상태**: M6 (마무리) 완료 ✅, WinUI 전환 PoC 착수

---

## 📋 개발 마일스톤 현황

| 마일스톤 | 설명 | 상태 |
|---------|------|------|
| M0 | Project Setup (프로젝트 초기화) | ✅ 완료 |
| M1 | Basic Map View (기본 지도 뷰) | ✅ 완료 |
| M2 | File Loading (파일 로딩) | ✅ 완료 |
| M3 | Layer Management (레이어 관리) | ✅ 완료 |
| M4 | Attribute Table (속성 테이블) | ✅ 완료 |
| M5 | Project File (프로젝트 파일) | ✅ 완료 |
| M6 | Polish & Optimization (마무리) | ✅ 완료 |

---

## 🏗️ 프로젝트 구조

```
SpatialView/
├── src/
│   ├── SpatialView/                    # 메인 WPF 앱
│   │   ├── App.xaml                    # 앱 리소스 및 테마
│   │   ├── App.xaml.cs                 # DI 컨테이너, 전역 예외 처리
│   │   ├── MainWindow.xaml             # 메인 윈도우 레이아웃
│   │   ├── MainWindow.xaml.cs          # 메인 윈도우 코드비하인드
│   │   ├── Views/
│   │   │   ├── Controls/
│   │   │   │   ├── AttributePanel.xaml(.cs)    # 속성 테이블 패널
│   │   │   │   └── MapControl.xaml(.cs)        # 지도 컨트롤
│   │   │   ├── Dialogs/
│   │   │   │   ├── AddFieldDialog.xaml(.cs)    # 필드 추가 다이얼로그
│   │   │   │   ├── ErrorDialog.xaml(.cs)       # 에러 다이얼로그
│   │   │   │   ├── FieldCalculatorDialog.xaml(.cs)  # 필드 계산기
│   │   │   │   └── SelectFieldDialog.xaml(.cs)      # 필드 선택 다이얼로그
│   │   │   ├── FeaturePopupWindow.xaml(.cs)    # 피처 속성 팝업
│   │   ├── ViewModels/
│   │   │   ├── MainViewModel.cs        # 메인 뷰모델
│   │   │   ├── MapViewModel.cs         # 지도 뷰모델
│   │   │   ├── LayerPanelViewModel.cs  # 레이어 패널 뷰모델
│   │   │   ├── LayerItemViewModel.cs   # 레이어 아이템 뷰모델
│   │   │   ├── AttributePanelViewModel.cs  # 속성 패널 뷰모델
│   │   │   └── FeaturePopupViewModel.cs    # 피처 팝업 뷰모델
│   │   ├── Converters/                 # XAML 값 변환기
│   │   ├── Themes/                     # MaterialDesign 테마
│   │   └── Assets/                     # 아이콘, 이미지
│   │
│   ├── SpatialView.Core/               # 핵심 비즈니스 로직
│   │   ├── Models/
│   │   │   ├── LayerInfo.cs            # 레이어 정보 모델
│   │   │   ├── ProjectFile.cs          # 프로젝트 파일 모델
│   │   │   └── ...
│   │   └── Services/Interfaces/
│   │       ├── IBaseMapService.cs      # 배경지도 서비스 인터페이스
│   │       ├── IDataLoaderService.cs   # 데이터 로더 서비스 인터페이스
│   │       ├── IDataProvider.cs        # 데이터 프로바이더 인터페이스
│   │       ├── ILoggingService.cs      # 로깅 서비스 인터페이스
│   │       └── IProjectService.cs      # 프로젝트 서비스 인터페이스
│   │
│   └── SpatialView.Infrastructure/     # 외부 연동, 데이터 액세스
│       ├── DataProviders/
│       │   ├── ShapefileDataProvider.cs    # Shapefile 로더
│       │   ├── GeoJsonDataProvider.cs      # GeoJSON 로더
│       │   └── FileGdbDataProvider.cs      # FileGDB 로더
│       └── Services/
│           ├── BaseMapService.cs       # 배경지도 서비스 구현
│           ├── DataLoaderService.cs    # 데이터 로더 서비스 구현
│           ├── LoggingService.cs       # 로깅 서비스 구현
│           └── ProjectService.cs       # 프로젝트 서비스 구현
│
└── docs/                               # 문서
    ├── 01_prd.md                       # 제품 요구사항
    ├── 05_design_system.md             # 디자인 시스템
    ├── 06_tasks_prompts.md             # 작업 프롬프트
    └── 개발상황.md                      # 이 문서
```

---

## ✅ 구현 완료 기능

### M0: Project Setup

- [x] Solution 및 Project 구조 생성
- [x] NuGet Package 설치 (SharpMap, MaterialDesign, NTS 등)
- [x] MVVM 패턴 설정 (CommunityToolkit.Mvvm)
- [x] DI Container 구성 (Microsoft.Extensions.DependencyInjection)
- [x] MaterialDesign 테마 적용

### M1: Basic Map View

- [x] MainWindow 레이아웃 (Toolbar, LayerPanel, MapView, StatusBar)
- [x] SharpMap MapBox 컨트롤 통합
- [x] 마우스 좌표 실시간 표시
- [x] 축척(Scale) 표시
- [x] 배경지도 (OpenStreetMap) 연동
- [x] 지도 도구: Pan, Zoom In, Zoom Out, Zoom Window, Select

### M2: File Loading

- [x] Shapefile (.shp) 로딩
- [x] GeoJSON (.geojson, .json) 로딩
- [x] FileGDB (.gdb) 로딩 (GDAL/OGR OpenFileGDB 드라이버)
- [x] Drag & Drop 파일 열기
- [x] 파일 열기 다이얼로그
- [x] 로딩 진행 표시
- [x] 비동기 로딩 (async/await)
- [x] CancellationToken 지원

### M3: Layer Management

- [x] 레이어 목록 표시 (LayerPanel)
- [x] 레이어 표시/숨김 토글
- [x] 레이어 삭제
- [x] 레이어 이름 변경
- [x] 레이어 순서 변경 (Drag & Drop)
- [x] 레이어 투명도 조절 (Slider)
- [x] 레이어 범위로 줌 (Zoom to Layer)
- [x] 컨텍스트 메뉴

### M4: Attribute Table

- [x] 속성 테이블 패널
- [x] DataGrid with Virtualization
- [x] 필터 검색
- [x] 피처 선택 및 Highlight
- [x] 피처로 줌 (Double-click)
- [x] **필드 추가** 다이얼로그
- [x] **필드 삭제** 기능
- [x] **필드 계산기** (수식 기반 필드 값 계산)
- [x] **피처 속성 팝업** (선택 피처 상세 보기)
- [x] **속성 복사/붙여넣기** (레이어 검증 포함)

### M5: Project File

- [x] 프로젝트 저장 (.svproj)
- [x] 프로젝트 열기
- [x] 맵 설정 저장/복원 (중심, 줌, CRS, 배경지도)
- [x] 레이어 설정 저장/복원 (경로, 스타일, 표시, 투명도, 순서)
- [x] 상대 경로 변환 (프로젝트 파일 기준)
- [x] 새 프로젝트 (현재 상태 초기화)

### M6: Polish & Optimization

- [x] Global Exception Handler
  - DispatcherUnhandledException
  - UnhandledException
  - UnobservedTaskException
- [x] **로깅 서비스** (LoggingService)
  - 로그 위치: `%LOCALAPPDATA%\SpatialView\Logs\`
  - 날짜별 로그 파일
  - 로그 레벨: Debug, Info, Warning, Error
  - 7일 이상 된 로그 자동 삭제
- [x] **에러 다이얼로그** (ErrorDialog)
  - 사용자 친화적 오류 메시지
  - 상세 정보 (Stack Trace) 접기/펼치기
  - 로그 폴더 열기 버튼
  - 클립보드 복사 버튼
- [x] 성능 최적화
  - CancellationToken 지원 (로딩 취소)
  - Progress 콜백 (로딩 진행 상황)
  - DataGrid Virtualization
  - Async 로딩

---

## 🛠️ 사용 기술 스택

### 프레임워크 및 런타임
- **.NET 10.0-windows** (주요 프로젝트)
- **WPF** (Windows Desktop)
- **WinUI 3** (별도 프로젝트 `src/SpatialView.WinUI`, 전환 준비 중)

### 주요 NuGet 패키지

| 패키지 | 버전 | 용도 |
|-------|------|------|
| CommunityToolkit.Mvvm | 8.4.0 | MVVM 패턴 |
| Microsoft.Extensions.DependencyInjection | 10.0.1 | DI |
| MaterialDesignThemes | 5.3.0 | WPF UI 테마 |
| SharpVectors | 1.8.5 | SVG 렌더링 |
| MaxRev.Gdal.Core | 3.12.0.427 | GDAL/OGR 코어 |
| MaxRev.Gdal.WindowsRuntime.Minimal | 3.12.0.427 | GDAL 네이티브 배포 |
| (GIS 엔진) SpatialView.Engine | - | 자체 GIS 엔진 추상화 |
| (어댑터) SpatialView.Infrastructure | - | SharpMap 어댑터 등 구현체 |

---

## 📁 지원 파일 형식

| 형식 | 확장자 | 읽기 | 쓰기 | 비고 |
|------|--------|------|------|------|
| Shapefile | .shp | ✅ | ❌ | 완전 지원 |
| GeoJSON | .geojson, .json | ✅ | ❌ | 완전 지원 |
| FileGDB | .gdb (폴더) | ✅ | ❌ | GDAL 기반 (OpenFileGDB 드라이버) |

### GDAL 설정
- **패키지**: MaxRev.Gdal.Core + MaxRev.Gdal.WindowsRuntime.Minimal (v3.12.0.427)
- **초기화**: App.xaml.cs에서 `GdalBase.ConfigureAll()` 호출
- **네이티브 DLL 위치**: `bin\Debug\net10.0-windows\win-x64\runtimes\win-x64\native\`
- **지원 드라이버**: OpenFileGDB (읽기), ESRI Shapefile, GeoJSON, GeoPackage 등 70개 이상 OGR 드라이버

### 배포 설정
배포 시 GDAL 네이티브 DLL이 자동으로 실행 파일과 같은 폴더에 복사됩니다.

```bash
# Release 배포
dotnet publish src/SpatialView/SpatialView.csproj -c Release -r win-x64 --self-contained false -o ./publish/SpatialView

# 자기 포함(Self-contained) 배포 (.NET Runtime 포함)
dotnet publish src/SpatialView/SpatialView.csproj -c Release -r win-x64 --self-contained true -o ./publish/SpatialView-Standalone
```

배포 폴더 구조:
```
publish/SpatialView/
├── SpatialView.exe           # 메인 실행 파일
├── SpatialView.dll
├── SpatialView.*.dll         # 프로젝트 DLL
├── gdal.dll                  # GDAL 네이티브 라이브러리 (22MB)
├── gdal_wrap.dll             # GDAL C# 래퍼
├── proj_9.dll                # PROJ 좌표계 변환
├── geos.dll                  # GEOS 공간 연산
├── spatialite.dll            # SpatiaLite
└── ... (기타 GDAL 의존 DLL ~270개 파일)

---

## 🎯 주요 UI 기능

### 메인 윈도우 레이아웃
```
+--------------------------------------------------+
|                    Toolbar                        |
|  [New] [Open] [Save] | [Pan] [ZoomIn] [Select]   |
+--------+-------------------------------------+----+
|        |                                     |    |
| Layer  |                                     | S  |
| Panel  |            Map View                 | t  |
| (280px)|                                     | a  |
|        |                                     | t  |
|        |                                     | u  |
+--------+                                     | s  |
|                                              |    |
|            Attribute Table Panel             |    |
|                                              |    |
+--------------------------------------------------+
|              Status Bar (좌표, 축척)              |
+--------------------------------------------------+
```

### 지도 도구
- **Pan**: 지도 이동 (기본)
- **Zoom In**: 클릭 확대
- **Zoom Out**: 클릭 축소
- **Zoom Window**: 드래그 영역 확대
- **Select**: 피처 선택

### 피처 속성 팝업
- 선택 피처의 모든 속성 표시
- 필드별 체크박스로 선택 복사
- 전체 선택/해제
- 복사/붙여넣기 (동일 레이어 검증)
- 피처로 줌

---

## 📝 알려진 이슈 및 제한사항

### 제한사항
1. **읽기 전용**: 현재 파일 저장/내보내기 기능 미구현
2. **SharpMap 호환성**: .NET Framework용 SharpMap.UI 사용 (NU1701 경고)
3. **좌표계**: WGS84 (EPSG:4326) 기본, 일부 좌표계 변환 제한

### 해결된 이슈
- [x] 필드 추가 다이얼로그 버튼 안 보임 → SizeToContent 적용
- [x] 체크박스 클릭 안됨 → DataGrid → ListView 변경
- [x] 필드 삭제 후 갱신 안됨 → 테이블 재설정
- [x] 선택 도구 아이콘 활성화 안됨 → OneWay 바인딩
- [x] ContextMenu 공유 오류 → x:Shared="False"

---

## 🚀 실행 방법

### 빌드
```bash
cd G:\Project\SpatialView
dotnet build src/SpatialView/SpatialView.csproj
```

### 실행
```bash
dotnet run --project src/SpatialView/SpatialView.csproj
```

### 로그 확인
```
%LOCALAPPDATA%\SpatialView\Logs\spatialview_YYYY-MM-DD.log
```

---

## 📊 코드 통계

| 항목 | 수량 |
|------|------|
| ViewModels | 6개 |
| Views/Controls | 2개 |
| Views/Dialogs | 4개 |
| Services | 4개 |
| DataProviders | 3개 |
| 인터페이스 | 5개 |

---

## 👥 개발팀

- **개발**: AI Assistant (Claude)
- **프레임워크**: Cursor AI IDE

---

## 📅 업데이트 이력

| 날짜 | 마일스톤 | 내용 |
|------|---------|------|
| 2025-12-31 | M0-M1 | 프로젝트 초기화, 기본 지도 뷰 |
| 2025-12-31 | M2 | 파일 로딩 (Shapefile, GeoJSON, FileGDB) |
| 2025-12-31 | M3 | 레이어 관리 |
| 2025-12-31 | M4 | 속성 테이블, 필드 관리 |
| 2025-12-31 | M5 | 프로젝트 파일 저장/로드 |
| 2025-12-31 | M6 | 에러 처리, 로깅, 성능 최적화, GDAL/FileGDB 지원 |
| 2026-01-07 | 빌드 환경 | 폴더명 # 문자 제거, .NET 10 환경 정리, WinUI 3 빌드 문제 해결 |

---

**문서 끝**

