<Window x:Class="SpatialView.Views.Dialogs.CoordinateTransformDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="레이어 좌표계 변환" 
        Height="500" Width="500"
        MinHeight="450" MinWidth="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        Background="{StaticResource BackgroundBrush}">
    
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="레이어 좌표계 변환" FontSize="18" FontWeight="SemiBold"
                       Foreground="{StaticResource TextPrimaryBrush}"/>
            <TextBlock Text="선택한 레이어의 좌표계를 변환합니다."
                       FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"
                       Margin="0,4,0,0"/>
        </StackPanel>
        
        <!-- Layer Selection -->
        <GroupBox Grid.Row="1" Header="레이어 선택" Margin="0,0,0,16">
            <StackPanel Margin="8">
                <ComboBox x:Name="LayerComboBox"
                          materialDesign:HintAssist.Hint="변환할 레이어 선택"
                          SelectionChanged="LayerComboBox_SelectionChanged"
                          DisplayMemberPath="Name"/>
                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                    <TextBlock Text="현재 좌표계: " Foreground="{StaticResource TextSecondaryBrush}"/>
                    <TextBlock x:Name="CurrentCrsText" Text="알 수 없음" FontWeight="Medium"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                    <TextBlock Text="피처 수: " Foreground="{StaticResource TextSecondaryBrush}"/>
                    <TextBlock x:Name="FeatureCountText" Text="0"/>
                </StackPanel>
            </StackPanel>
        </GroupBox>
        
        <!-- Target Coordinate System -->
        <GroupBox Grid.Row="2" Header="대상 좌표계" Margin="0,0,0,16">
            <StackPanel Margin="8">
                <ComboBox x:Name="TargetCrsComboBox"
                          materialDesign:HintAssist.Hint="변환할 좌표계 선택"
                          SelectionChanged="TargetCrsComboBox_SelectionChanged"/>
                <TextBlock x:Name="TargetCrsDescText" 
                           Text="" 
                           FontSize="11" 
                           Foreground="{StaticResource TextSecondaryBrush}"
                           TextWrapping="Wrap"
                           Margin="0,8,0,0"/>
            </StackPanel>
        </GroupBox>
        
        <!-- Options -->
        <GroupBox Grid.Row="3" Header="출력 옵션" Margin="0,0,0,16">
            <StackPanel Margin="8">
                <RadioButton x:Name="AddToMapRadio"
                             Content="지도에 새 레이어로 추가"
                             IsChecked="True"
                             GroupName="OutputOption"
                             Margin="0,0,0,8"/>
                <StackPanel Orientation="Horizontal" 
                            IsEnabled="{Binding IsChecked, ElementName=AddToMapRadio}"
                            Margin="20,0,0,8">
                    <TextBlock Text="레이어 이름: " VerticalAlignment="Center"/>
                    <TextBox x:Name="NewLayerNameTextBox" 
                             Width="180"
                             materialDesign:HintAssist.Hint="레이어 이름"/>
                </StackPanel>
                
                <RadioButton x:Name="SaveToFileRadio"
                             Content="파일로 저장"
                             GroupName="OutputOption"
                             Margin="0,0,0,8"/>
                <Grid IsEnabled="{Binding IsChecked, ElementName=SaveToFileRadio}"
                      Margin="20,0,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="OutputPathTextBox" 
                             Grid.Column="0"
                             IsReadOnly="True"
                             materialDesign:HintAssist.Hint="저장 경로"/>
                    <Button Grid.Column="1" 
                            Content="찾아보기..."
                            Click="BrowseOutputPath_Click"
                            Margin="8,0,0,0"
                            Padding="8,4"/>
                </Grid>
            </StackPanel>
        </GroupBox>
        
        <!-- Progress -->
        <GroupBox Grid.Row="4" Header="변환 상태">
            <Grid Margin="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <ProgressBar x:Name="TransformProgressBar" 
                             Grid.Row="0"
                             Height="8"
                             Value="0"
                             Margin="0,0,0,8"/>
                
                <TextBlock x:Name="ProgressText" 
                           Grid.Row="1"
                           Text="대기 중..."
                           Foreground="{StaticResource TextSecondaryBrush}"/>
                
                <TextBox x:Name="LogTextBox"
                         Grid.Row="2"
                         IsReadOnly="True"
                         VerticalScrollBarVisibility="Auto"
                         TextWrapping="Wrap"
                         Background="#F5F5F5"
                         Margin="0,8,0,0"
                         FontFamily="Consolas"
                         FontSize="11"/>
            </Grid>
        </GroupBox>
        
        <!-- Buttons -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button x:Name="TransformButton"
                    Content="변환 실행" 
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Click="TransformButton_Click"
                    Width="100" Margin="0,0,8,0"/>
            <Button Content="닫기" 
                    Style="{StaticResource MaterialDesignFlatButton}"
                    Click="CloseButton_Click"
                    Width="80"/>
        </StackPanel>
    </Grid>
</Window>
